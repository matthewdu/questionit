<% provide(:title, "Userlist") %>
<h1>All Users</h1>

<%= will_paginate %>

<ul>
	<% @users.each do |user| %>
		<li>
			<%= user.id %>
			<%= link_to(user.username,user) %>
			<%= user.name %>
		</li>
	<% end %>
</ul>

<%= will_paginate %>